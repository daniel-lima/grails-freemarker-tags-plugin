FreeMarker reserved words present in Grails Core TagLibs that can cause errors to FreeMarker's parser, will be preceeded by '_' (underscore).

GSP

{code: xml}
<g:renderErrors bean="${myUserInstance}" as="list" />
{code}


FreeMarker template

{code: xml}
<@g.renderErrors bean=myUserInstance _as="list" />
{code}

or

{code: xml}
\[@g.renderErrors bean=myUserInstance _as="list" /]
{code}

